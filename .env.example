# PengyStream Configuration

# Comma-separated paths to watch for video files
# Windows: Use forward slashes (C:/Videos) or escaped backslashes (C:\\Videos)
# Linux/Mac: /path/to/Movies
MOVIE_FOLDERS=C:/Users/YourName/Videos/Movies,D:/Media/TV Shows

# Maximum number of simultaneous encodes
MAX_ENCODES=2

# Video codec (default: h264)
VIDEO_CODEC=h264

# Maximum resolution (e.g., 1080p, 1440p, 2160p)
MAX_RESOLUTION=1440p

# Audio codec (default: aac)
AUDIO_CODEC=aac

# CPU threshold percentage (0-100) - pause encoding if exceeded
CPU_THRESHOLD=80

# GPU threshold percentage (0-100) - pause encoding if exceeded
# Requires NVIDIA GPU with drivers on Windows/Linux
# Set to 100 to disable GPU monitoring
GPU_THRESHOLD=80

# Poll interval in seconds for checking new files (when watchdog events are slow)
POLL_INTERVAL=60

# Copy streams if already compatible (true/false)
COPY_IF_COMPATIBLE=true

# Log file path (optional, defaults to pengystream.log)
LOG_FILE=pengystream.log
